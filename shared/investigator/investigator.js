class Investigator {

    get dex() {
        return this._dex || 0;
    }

    set dex(val) {
        this._dex = val;
    }

    get str() {
        return this._str || 0;
    }

    set str(val) {
        this._str = val;
    }

    get int() {
        return this._int || 0;
    }

    set int(val) {
        this._int = val;
    }

    get con() {
        return this._con || 0;
    }

    set con(val) {
        this._con = val;
    }

    get app() {
        return this._app || 0;
    }

    set app(val) {
        this._app = val;
    }

    get pow() {
        return this._pow || 0;
    }

    set pow(val) {
        this._pow = val;
    }

    get siz() {
        return this._siz || 0;
    }

    set siz(val) {
        this._siz = val;
    }

    get edu() {
        return this._edu || 0;
    }

    set edu(val) {
        this._edu = val;
    }
}